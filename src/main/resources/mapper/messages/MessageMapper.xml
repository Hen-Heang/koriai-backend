<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.messages.mapper.MessageMapper">

    <resultMap id="MessageResultMap" type="com.heang.koriaibackend.domain.messages.model.Message">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="corrections" column="corrections"/>
        <result property="tokensUsed" column="tokens_used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, conversation_id, role, content, corrections, tokens_used, created_at
    </sql>

    <insert id="insert" parameterType="com.heang.koriaibackend.domain.messages.model.Message"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO messages (
            conversation_id,
            role,
            content,
            corrections,
            tokens_used
        ) VALUES (
            #{conversationId},
            #{role},
            #{content},
            CAST(#{corrections} AS jsonb),
            #{tokensUsed}
        )
    </insert>

    <select id="findById" resultMap="MessageResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM messages
        WHERE id = #{id}
    </select>

    <select id="findByConversationId" resultMap="MessageResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM messages
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByConversationId" resultType="int">
        SELECT COUNT(1)
        FROM messages
        WHERE conversation_id = #{conversationId}
    </select>

    <delete id="deleteByConversationId">
        DELETE FROM messages
        WHERE conversation_id = #{conversationId}
    </delete>
</mapper>
