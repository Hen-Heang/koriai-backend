<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.foundation.mapper.FoundationProgressMapper">

    <resultMap id="FoundationProgressResultMap" type="com.heang.koriaibackend.domain.foundation.model.FoundationProgress">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="lessonId" column="lesson_id"/>
        <result property="track" column="track"/>
        <result property="completed" column="completed"/>
        <result property="accuracy" column="accuracy"/>
        <result property="attempts" column="attempts"/>
        <result property="lastAttemptAt" column="last_attempt_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="selectColumns">
        id, user_id, lesson_id, track, completed, accuracy, attempts,
        last_attempt_at, created_at, updated_at
    </sql>

    <select id="findByUser" resultMap="FoundationProgressResultMap">
        SELECT
        <include refid="selectColumns"/>
        FROM foundation_progress
        WHERE user_id = #{userId}
    </select>

    <select id="findByUserAndLesson" resultMap="FoundationProgressResultMap">
        SELECT
        <include refid="selectColumns"/>
        FROM foundation_progress
        WHERE user_id = #{userId}
          AND lesson_id = #{lessonId}
    </select>

    <insert id="upsert" parameterType="com.heang.koriaibackend.domain.foundation.model.FoundationProgress">
        INSERT INTO foundation_progress (user_id, lesson_id, track, completed, accuracy, attempts, last_attempt_at)
        VALUES (#{userId}, #{lessonId}, #{track}, #{completed}, #{accuracy}, 1, NOW())
        ON CONFLICT (user_id, lesson_id) DO UPDATE SET
            completed       = foundation_progress.completed OR EXCLUDED.completed,
            accuracy        = GREATEST(foundation_progress.accuracy, EXCLUDED.accuracy),
            attempts        = foundation_progress.attempts + 1,
            track           = EXCLUDED.track,
            last_attempt_at = NOW(),
            updated_at      = NOW()
    </insert>

</mapper>
