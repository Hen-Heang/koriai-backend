<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.note.mapper.NoteMapper">

    <resultMap id="NoteResultMap" type="com.heang.koriaibackend.domain.note.model.Note">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="slug" column="slug"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="icon" column="icon"/>
        <result property="category" column="category"/>
        <result property="tags" column="tags"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insertNote" parameterType="com.heang.koriaibackend.domain.note.model.Note"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notes (user_id, slug, title, description, icon, category, tags, content)
        VALUES (#{userId}, #{slug}, #{title}, #{description}, #{icon}, #{category},
                CAST(#{tags} AS jsonb), #{content})
    </insert>

    <update id="updateNote" parameterType="com.heang.koriaibackend.domain.note.model.Note">
        UPDATE notes
        SET title       = #{title},
            description = #{description},
            icon        = #{icon},
            category    = #{category},
            tags        = CAST(#{tags} AS jsonb),
            content     = #{content},
            updated_at  = NOW()
        WHERE user_id = #{userId}
          AND slug = #{slug}
    </update>

    <delete id="deleteByUserAndSlug">
        DELETE
        FROM notes
        WHERE user_id = #{userId}
          AND slug = #{slug}
    </delete>

    <select id="findByUserAndSlug" resultMap="NoteResultMap">
        SELECT id, user_id, slug, title, description, icon, category,
               tags::text AS tags,
               content, created_at, updated_at
        FROM notes
        WHERE user_id = #{userId}
          AND slug = #{slug}
    </select>

    <select id="findMetaByUserId" resultMap="NoteResultMap">
        SELECT id, user_id, slug, title, description, icon, category,
               tags::text AS tags,
               created_at, updated_at
        FROM notes
        WHERE user_id = #{userId}
        ORDER BY updated_at DESC
    </select>

</mapper>
