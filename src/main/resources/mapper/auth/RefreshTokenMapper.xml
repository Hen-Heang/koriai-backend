<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.auth.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenResultMap" type="com.heang.koriaibackend.domain.auth.model.RefreshToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="revokedAt" column="revoked_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.heang.koriaibackend.domain.auth.model.RefreshToken"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (
            user_id,
            token_hash,
            expires_at
        ) VALUES (
            #{userId},
            #{tokenHash},
            #{expiresAt}
        )
    </insert>

    <select id="findByTokenHash" resultMap="RefreshTokenResultMap">
        SELECT id, user_id, token_hash, expires_at, revoked_at, created_at
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="revokeByTokenHash">
        UPDATE refresh_tokens
        SET revoked_at = NOW()
        WHERE token_hash = #{tokenHash}
          AND revoked_at IS NULL
    </update>

    <update id="revokeAllByUserId">
        UPDATE refresh_tokens
        SET revoked_at = NOW()
        WHERE user_id = #{userId}
          AND revoked_at IS NULL
    </update>

    <delete id="deleteExpired">
        DELETE FROM refresh_tokens
        WHERE expires_at &lt; NOW()
           OR revoked_at IS NOT NULL
    </delete>
</mapper>
