<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.listening.mapper.ListeningMapper">

    <resultMap id="ListeningLessonResultMap" type="com.heang.koriaibackend.domain.listening.model.ListeningLesson">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="topic" column="topic"/>
        <result property="title" column="title"/>
        <result property="level" column="level"/>
        <result property="transcript" column="transcript"/>
        <result property="quiz" column="quiz"/>
        <result property="modelUsed" column="model_used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertLesson" parameterType="com.heang.koriaibackend.domain.listening.model.ListeningLesson"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO listening_lessons (user_id, topic, title, level, transcript, quiz, model_used)
        VALUES (#{userId}, #{topic}, #{title}, #{level}, CAST(#{transcript} AS jsonb), CAST(#{quiz} AS jsonb), #{modelUsed})
    </insert>

    <select id="findLessonByIdAndUser" resultMap="ListeningLessonResultMap">
        SELECT id, user_id, topic, title, level, transcript::text AS transcript, quiz::text AS quiz, model_used, created_at
        FROM listening_lessons
        WHERE id = #{id}
          AND user_id = #{userId}
    </select>

    <select id="findLessonsByUserId" resultMap="ListeningLessonResultMap">
        SELECT id, user_id, topic, title, level, transcript::text AS transcript, quiz::text AS quiz, model_used, created_at
        FROM listening_lessons
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insertAttempt" parameterType="com.heang.koriaibackend.domain.listening.model.ListeningAttempt"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO listening_attempts (user_id, lesson_id, score, total, accuracy, completed)
        VALUES (#{userId}, #{lessonId}, #{score}, #{total}, #{accuracy}, #{completed})
    </insert>

    <select id="countCompletedAttempts" resultType="int">
        SELECT COUNT(*) FROM listening_attempts
        WHERE user_id = #{userId}
          AND completed = TRUE
    </select>

</mapper>
