<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.goal.mapper.TaskMapper">

    <resultMap id="TaskResultMap" type="com.heang.koriaibackend.domain.goal.model.Task">
        <id property="id" column="id"/>
        <result property="goalId" column="goal_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="completed" column="completed"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="dailyStartTime" column="daily_start_time"/>
        <result property="dailyEndTime" column="daily_end_time"/>
        <result property="anytime" column="is_anytime"/>
        <result property="durationMinutes" column="duration_minutes"/>
        <result property="color" column="color"/>
        <result property="tags" column="tags"
                typeHandler="com.heang.koriaibackend.common.mybatis.StringListTypeHandler"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, goal_id, user_id, title, description, completed, start_date, end_date,
        daily_start_time, daily_end_time, is_anytime, duration_minutes, color,
        tags, updated_by, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.heang.koriaibackend.domain.goal.model.Task">
        INSERT INTO tasks (
            id, goal_id, user_id, title, description, completed,
            start_date, end_date, daily_start_time, daily_end_time,
            is_anytime, duration_minutes, color, tags, updated_by
        ) VALUES (
            #{id}, #{goalId}, #{userId}, #{title}, #{description}, #{completed},
            #{startDate}, #{endDate}, #{dailyStartTime}, #{dailyEndTime},
            #{anytime}, #{durationMinutes}, #{color},
            #{tags, typeHandler=com.heang.koriaibackend.common.mybatis.StringListTypeHandler, jdbcType=ARRAY},
            #{updatedBy}
        )
    </insert>

    <select id="findById" resultMap="TaskResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM tasks
        WHERE id = #{id}
    </select>

    <select id="findByGoal" resultMap="TaskResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM tasks
        WHERE goal_id = #{goalId}
        ORDER BY start_date NULLS LAST, created_at
    </select>

    <select id="findRange" resultMap="TaskResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM tasks
        <choose>
            <when test="goalId != null">
                WHERE goal_id = #{goalId}
            </when>
            <otherwise>
                WHERE user_id = #{userId}
            </otherwise>
        </choose>
        <if test="from != null">AND start_date &gt;= #{from}</if>
        <if test="to != null">AND start_date &lt;= #{to}</if>
        ORDER BY start_date NULLS LAST, created_at
    </select>

    <update id="update" parameterType="com.heang.koriaibackend.domain.goal.model.Task">
        UPDATE tasks
        SET title            = #{title},
            description      = #{description},
            completed        = #{completed},
            start_date       = #{startDate},
            end_date         = #{endDate},
            daily_start_time = #{dailyStartTime},
            daily_end_time   = #{dailyEndTime},
            is_anytime       = #{anytime},
            duration_minutes = #{durationMinutes},
            color            = #{color},
            tags             = #{tags, typeHandler=com.heang.koriaibackend.common.mybatis.StringListTypeHandler, jdbcType=ARRAY},
            updated_by       = #{updatedBy},
            updated_at       = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateCompleted">
        UPDATE tasks
        SET completed  = #{completed},
            updated_by = #{updatedBy},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tasks WHERE id = #{id}
    </delete>

</mapper>
