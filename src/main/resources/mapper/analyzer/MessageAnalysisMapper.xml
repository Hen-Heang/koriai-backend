<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.analyzer.mapper.MessageAnalysisMapper">

    <resultMap id="MessageAnalysisResultMap" type="com.heang.koriaibackend.domain.analyzer.model.MessageAnalysis">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="source" column="source"/>
        <result property="originalText" column="original_text"/>
        <result property="literalMeaning" column="literal_meaning"/>
        <result property="naturalMeaning" column="natural_meaning"/>
        <result property="businessContext" column="business_context"/>
        <result property="politenessLevel" column="politeness_level"/>
        <result property="tone" column="tone"/>
        <result property="breakdown" column="breakdown"/>
        <result property="suggestedReplies" column="suggested_replies"/>
        <result property="modelUsed" column="model_used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.heang.koriaibackend.domain.analyzer.model.MessageAnalysis"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message_analysis (
            user_id,
            source,
            original_text,
            literal_meaning,
            natural_meaning,
            business_context,
            politeness_level,
            tone,
            breakdown,
            suggested_replies,
            model_used
        ) VALUES (
            #{userId},
            #{source},
            #{originalText},
            #{literalMeaning},
            #{naturalMeaning},
            #{businessContext},
            #{politenessLevel},
            #{tone},
            CAST(#{breakdown} AS jsonb),
            CAST(#{suggestedReplies} AS jsonb),
            #{modelUsed}
        )
    </insert>

    <select id="findByUserId" resultMap="MessageAnalysisResultMap">
        SELECT id, user_id, source, original_text, literal_meaning, natural_meaning,
               business_context, politeness_level, tone, breakdown, suggested_replies,
               model_used, created_at
        FROM message_analysis
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
</mapper>
