<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.koriaibackend.domain.push.mapper.TelegramLinkMapper">

    <resultMap id="TelegramLinkResultMap" type="com.heang.koriaibackend.domain.push.model.TelegramLink">
        <id property="userId" column="user_id"/>
        <result property="chatId" column="chat_id"/>
        <result property="linkCode" column="link_code"/>
        <result property="linkedAt" column="linked_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="upsertLinkCode">
        INSERT INTO user_telegram_links (user_id, link_code)
        VALUES (#{userId}, #{linkCode})
        ON CONFLICT (user_id)
        DO UPDATE SET link_code = EXCLUDED.link_code
    </insert>

    <select id="findByUserId" resultMap="TelegramLinkResultMap">
        SELECT user_id, chat_id, link_code, linked_at, created_at
        FROM user_telegram_links
        WHERE user_id = #{userId}
    </select>

    <select id="findByLinkCode" resultMap="TelegramLinkResultMap">
        SELECT user_id, chat_id, link_code, linked_at, created_at
        FROM user_telegram_links
        WHERE link_code = #{linkCode}
    </select>

    <update id="confirmChat">
        UPDATE user_telegram_links
        SET chat_id = #{chatId}, link_code = NULL, linked_at = NOW()
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteByUserId">
        DELETE FROM user_telegram_links WHERE user_id = #{userId}
    </delete>

</mapper>
